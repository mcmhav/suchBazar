# SuchWow.

Run by first running:

`go build -o predict`

Then:

`./predict`

## Dependencies

Need to run a PostgreSQL server with a table called `sobazar` containing all
events. A script for making this table is available at `../psql/createdb.sh`.
